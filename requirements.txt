# Pinned runtime dependencies for reproducible installs.
# Regenerate with: pip install -e . && pip freeze
Jinja2==3.1.6
MarkupSafe==3.0.3
pypdf==6.19.0
Pillow==12.3.0
jsonschema==4.26.0
weasyprint==70.0
